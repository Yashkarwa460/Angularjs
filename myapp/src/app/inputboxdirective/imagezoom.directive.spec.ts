import { ImagezoomDirective } from './imagezoom.directive';

describe('ImagezoomDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagezoomDirective();
    expect(directive).toBeTruthy();
  });
});
