import { MagicsDirective } from './magic.directive';

describe('MagicsDirective', () => {
  it('should create an instance', () => {
    const directive = new MagicsDirective();
    expect(directive).toBeTruthy();
  });
});
